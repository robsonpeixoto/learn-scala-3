pre-push:
  commands:
    compile:
      glob: "{*.scala,build.sbt}"
      run: sbt compile
pre-commit:
  commands:
    format-scala:
      glob: "{*.scala,build.sbt}"
      run: sbt scalafmtOnly {staged_files} && git add {staged_files}
